# GitHub Packages configuration for @arzancloud scope
@arzancloud:registry=https://npm.pkg.github.com

# Authentication is done via GITHUB_TOKEN in CI or npm login for local
# For local development, run: npm login --registry=https://npm.pkg.github.com --scope=@arzancloud
